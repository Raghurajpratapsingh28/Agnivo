{
  "name": "@agnivo/api",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/api",
    "build": "go build -o bin/api ./cmd/api",
    "test": "go test ./...",
    "lint": "go vet ./..."
  }
}

{
  "name": "@agnivo/cron",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/cron",
    "build": "go build -o bin/cron ./cmd/cron",
    "test": "go test ./...",
    "lint": "go vet ./..."
  }
}

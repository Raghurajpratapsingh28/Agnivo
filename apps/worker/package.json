{
  "name": "@agnivo/worker",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/worker",
    "build": "go build -o bin/worker ./cmd/worker",
    "test": "go test ./...",
    "lint": "go vet ./..."
  }
}
